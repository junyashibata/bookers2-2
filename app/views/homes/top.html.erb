<div class="container">
  <div class="row">
    <div class="mx-auto">
      <h1>
        welcome to<i class="fas fa-book"></i><strong>Bookers</strong>!!
      </h1>
      <p>
        in<i class="fas fa-book"></i><strong>Bookers</strong>,</br>
        you can share and exchange your opinions , inpressions , and emotions</br>
        about various books!
      </p>
      <% if user_signed_in? %>
      <%= link_to 'Log in', user_path(current_user.id), class: "btn btn-secondary" %>
      <% else %>
      <%= link_to 'Log in', user_session_path, class: "btn btn-secondary" %>
      <% end %>
      <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>

